name: Build on Push

on:
  push:
    branches:
      - main
      - LibraryImport

jobs:
  call-publish-workflow:
    uses: ./.github/workflows/publish.yml
    with:
      skip_nuget: true
    secrets: inherit
